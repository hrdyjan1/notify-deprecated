import {StyleSheet} from 'react-native';
import {FontFamily} from '../../../constants/helpers/fonts';

const styles = StyleSheet.create({
  default: {
    fontFamily: FontFamily['serif-pro'],
  },
  navHeader: {
    fontFamily: FontFamily['wendy-one'],
  },
});

export {styles};
